fn makeClosure(x) {
    fn innerClosure() {
        fn closure() {
            return x;
        }
        return closure;
    }

    return innerClosure();
}

let x = makeInnerClosure(10);
let y = x();
let z = y();
