it("Atoms are consistently evaluated", || => {
   assert_eq(0, 0);
   assert_eq(1, 1);
   assert_eq(2, 2);
   assert_eq(3, 3);
   assert_eq(4, 4);
   assert_eq(5, 5);
   assert_eq(6, 6);
   assert_eq(7, 7);
   assert_eq(8, 8);
   assert_eq(9, 9);
   assert_eq(false, false);
   assert_eq(true, true);
   assert_eq(null, null);
   assert_eq("foo", "foo");
   assert_eq("", "");
});
