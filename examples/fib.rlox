fn fib(n) {
    if n <= 0 {
        return 0;
    };

    if n == 1 {
        return 1;
    };

    return fib(n - 1) + fib(n - 2);
}

var start = clock();
print fib(25);
var end = clock();
print (end - start) / 1000;